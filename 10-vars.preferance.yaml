-name: name
 hosts: localhost
 connection: local
  vars:
   name: "sundar"
  vars_file:
   name: "suman"
  vars_prompt:
   name: "sukesh"   
tasks:
  - name: print
   vars:
     name: "ramesh"
    debug:
      msg: "{{ name }}"
